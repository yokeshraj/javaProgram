package com.webservice.restful.webservice;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class RestfulWebserviceApplication {

	public static void main(String[] args) {
		SpringApplication.run(RestfulWebserviceApplication.class, args);
	}

}
//http://localhost:8080/swagger-ui/index.html->for swagger api
//for viewving links we use actuator
//for viewing visual status of links we use hal
//hateoas for checking app status